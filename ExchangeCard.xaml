<UserControl x:Class="CurrencyConversion.ExchangeCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CurrencyConversion"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"

             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>
    
    <Grid>
        <materialDesign:Card
             Background="#fafafa"
             Foreground="{DynamicResource PrimaryHueLightForegroundBrush}"
             Padding="0"
             Height="250"
             Width="600">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition
                        Height="100" />
                    <RowDefinition
                        Height="50" />
                    <RowDefinition
                        Height="100" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <ComboBox
                        Width="70"
                        HorizontalContentAlignment="Center"
                        HorizontalAlignment="Left"
                        Margin="56 16 16 4"
                        ItemsSource="{Binding Source={StaticResource currencyEnum}}"
                        SelectedItem="{Binding ExchangeFromCurrency}"
                        materialDesign:HintAssist.Hint="Select currency">
                    </ComboBox>
                    
                    <TextBox
                        HorizontalAlignment="Right"
                        MinWidth="90"
                        Width="auto"
                        MaxWidth="250"
                        Height="50"
                        VerticalContentAlignment="Center"
                        FontSize="20"
                        Margin="0,20,100,30"
                        Text="{Binding AmmountToExchange, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Ammount"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                </Grid>
                
                
                <Separator
                    Grid.Row="1"
                    Background="WhiteSmoke"
                    Style="{StaticResource MaterialDesignDarkSeparator}"/>

                <Grid Grid.Row="1">
                    <materialDesign:Chip 
                        VerticalAlignment="Center" 
                        Margin="120,0,0,0" 
                        Visibility="{Binding IsRateVisible, Converter={StaticResource BoolToVisConverter}, UpdateSourceTrigger=PropertyChanged}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon
                                Kind="TrendingUp"
                                VerticalAlignment="Center"
                                Height="24"
                                Width="24" />
                            <TextBlock Text="{Binding DisplayRate, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="5,0,0,0" />
                        </StackPanel>
                    </materialDesign:Chip>

                    <Button
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Margin="0,0,50,0"
                        Command="{Binding ExchangeCommand}"
                        Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                        ToolTip="Do shit">
                        <materialDesign:PackIcon
                        Kind="SwapVertical"
                        Height="24"
                        Width="24" />
                    </Button>
                </Grid>

                <Grid Grid.Row="2">
                    <ComboBox                   
                    Width="70"
                    HorizontalAlignment="Left"
                    HorizontalContentAlignment="Center"
                    Margin="56 16 16 4"
                    ItemsSource="{Binding Source={StaticResource currencyEnum}}"
                    SelectedItem="{Binding ExchangeToCurrency}"
                    materialDesign:HintAssist.Hint="Select currency">
                    </ComboBox>

                    <TextBlock 
                        HorizontalAlignment="Right"
                        MinWidth="90"
                        MaxWidth="250"
                        Width="auto"
                        Height="50"
                        Text="{Binding ExchangedAmmount, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,20,100,30"                        
                        FontSize="20"/>
                </Grid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
